services:
  - type: web
    name: mca-simulation-results-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --threads 4 --timeout 120 --keep-alive 5
    healthCheckPath: /health
